name: continuous-integration
on:
  workflow_call:
    inputs:
      component_name:
        type: string
        required: true
      docker_build:
        type: string
jobs:
  ci:
    runs-on: ubuntu-latest
    steps:
      - name: git-clone
        run: |
          echo "This is component - ${{ inputs.component_name }}"